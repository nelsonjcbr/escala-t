<div class="card mb-3">

  <p style="color: green"><%= notice %></p>

  <div class="card-header">
    <div class="row flex-between-end">
      <div class="col-auto align-self-center">
        <h5 class="mb-0 card-title" data-anchor="data-anchor">Estabelecimentos de Saúde</h5>
      </div>
      <div class="col-auto ms-auto">
        <%= link_to "Novo", new_estabelecimento_path, class: "btn btn-success me-1 mb-1" %> 
      </div>
    </div>
  </div>
  <div class="card-body bg-light">
    <div class="row match-height">
      <% @estabelecimentos.each do |estabelecimento| %>
        <div class="col-md-6 col-12">
          <%= render "estabelecimento_index", estabelecimento: estabelecimento %>
        </div>
      <% end %>
    </div>
  </div>
</div>
