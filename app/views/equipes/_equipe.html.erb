<%= turbo_frame_tag equipe do %>

  <div class="equipe-card">
    <div class="row match-height">
      <div class="col-6 col-md-3">
        <b><%= link_to equipe.nome, equipe_path(equipe) %></b>
      </div>
      <div class="col-6 col-md-3">
        <%= equipe.unidade.nome %>
      </div>
      <div class="col-6 col-md-2">
        <%= link_to "#{equipe.membros.count} membros", equipe_membros_path(equipe), data: { turbo: :false } %>
      </div>
      <div class="col-3 col-md-2">
        <%= link_to "Editar", edit_equipe_path(equipe), class: "btn btn-primary" %>
      </div>
      <div class="col-9 col-md-2">
        <%= button_to "Excluir", equipe, form: { data: { turbo_confirm: "Você tem certeza que deseja excluir?" } }, method: :delete,  class: "btn btn-danger" %>
      </div>
    </div>
    <hr>

  </div>

<% end %>