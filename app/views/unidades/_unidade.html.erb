<div id="<%= dom_id unidade %>">

<section id="caixa-inteira">
  <div class="row match-height">
    <div class="col-12">

      <div class="card">
        <div class="card-body"> 

          <div class="row">
            <div class="col-md-6">
              <label>Unidade</label>
            </div>
            <div class="col-md-6">
              <strong><%= unidade.nome %></strong>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="col-12 d-flex justify-content-end">
  <%= link_to "Editar", edit_estabelecimento_unidade_path(@estabelecimento, unidade), class: "btn btn-primary me-1 mb-1" %>
  <%= button_to "Excluir", estabelecimento_unidade_url(@estabelecimento, unidade), form: { data: { turbo_confirm: "Você tem certeza que deseja excluir?" } }, method: :delete,  class: "btn btn-danger me-1 mb-1" %>
</div>

