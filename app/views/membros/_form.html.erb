'form_with(url: equipe_membros_path(@equipe, membro), method: "post") do |form|'
<%= form_for([@equipe, membro]) do |form| %>
  <% if membro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(membro.errors.count, "error") %> prohibited this equipe from being saved:</h2>

      <ul>
        <% membro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='col-md-6 col-12'>
    <%= form.label :user_id, 'Usuário', style: "display: block" %>
    <%= form.select :user_id, User.all.collect { |i| [i.name, i.id] }, {prompt: 'Selecione'} , { class: "form-control form-select", required: true } %>
  </div>
  
  <div class="col-12 d-flex justify-content-end">
    <%= form.submit "Gravar", class: "btn btn-primary me-1 mb-1" %>
    <%= link_to "Cancelar", equipe_membros_path(@equipe), class: "btn btn-light-secondary me-1 mb-1" %>
  </div>
<% end %>
